# Lung_Cancer_Prediction_Using_ML
Using supervised algorithms( Decision tree, Logistic regression, Random forest , SVM, Naive baye, K-Nearest Neighbor classification) to explore for a healthcare dataset. 
